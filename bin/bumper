#!/usr/bin/env ruby
require 'optparse'
require 'bumper'

options = Hash.new

opts = OptionParser.new do |opts|
  opts.banner = %q[Bumper]

  opts.on('-p', '--project PROJECT_PATH', 'Set path to gem bump project') do |project|
    options[:project] = project
  end
end

opts.parse!

Bumper::Bump.perform(
  Bumper::Configuration.configure!(options)
)